export type RegularInputProps = {
  label: string;
  id: string;
  type: string;
  name: string;
  onChangeEvt: VoidFunction;
}

export type PasswordInputProps = {
  label: string;
  id: string;
  name: string;
  onChangeEvt: VoidFunction;
}